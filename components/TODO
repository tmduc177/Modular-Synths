- Add a method that returns all non-dependent properties for components;
    - Partially done in BaseComponent
- Modify constructor so that it has the ability to copy non-dependent properties from other components of the same type;
    - Partialy done in BaseComponent
